<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.elliottsj.ftw">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.READ_SYNC_SETTINGS" />
    <uses-permission android:name="android.permission.WRITE_SYNC_SETTINGS" />
    <uses-permission android:name="android.permission.READ_SYNC_STATS" />
    <uses-permission android:name="android.permission.AUTHENTICATE_ACCOUNTS" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <!-- TODO: Remove this once L is released -->
    <uses-sdk
        tools:node="replace" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyBMYeFpZyFFEgyTo93KuZBoiPA5kwjRtcM" />

        <activity
            android:name="com.elliottsj.ftw.activities.StopsActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.elliottsj.ftw.activities.MapActivity"
            android:parentActivityName="com.elliottsj.ftw.activities.StopsActivity" />
        <activity
            android:name="com.elliottsj.ftw.activities.AddStopActivity"
            android:label="@string/title_activity_add_stop"
            android:parentActivityName="com.elliottsj.ftw.activities.StopsActivity" />

        <provider
            android:name="com.elliottsj.ftw.provider.NextbusProvider"
            android:authorities="com.elliottsj.ftw.provider"
            android:exported="false"
            android:syncable="true" />

        <service android:name="com.elliottsj.ftw.sync.AuthenticatorService" >
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator" />
            </intent-filter>

            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/authenticator" />
        </service>
        <service
            android:name="com.elliottsj.ftw.sync.SyncService"
            android:exported="true" >

            <!-- android:process=":sync" (run in same process for debugging) -->
            <intent-filter>
                <action android:name="android.content.SyncAdapter" />
            </intent-filter>

            <meta-data
                android:name="android.content.SyncAdapter"
                android:resource="@xml/syncadapter" />
        </service>

        <receiver
            android:name=".pebble.PebbleDataReceiver"
            android:exported="true" >
            <intent-filter>
                <action android:name="com.getpebble.action.app.RECEIVE" />
            </intent-filter>
        </receiver>

        <service
            android:name=".pebble.PebbleSyncService"
            android:exported="false" >
        </service>
    </application>

</manifest>
